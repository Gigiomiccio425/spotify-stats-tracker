org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Versione mostrata all'utente. Si alza a mano quando cambia qualcosa che vale
# la pena annunciare; il numero interno con cui Android decide se un APK e' piu'
# recente e' invece il conteggio dei commit, calcolato in build.gradle.kts.
appVersionName=0.2.0
